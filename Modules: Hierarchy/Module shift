module top_module ( input clk, input d, output q );
    logic out1, out2, out3;
    my_dff u1(.clk(clk), .d(d), .q(out1));
    my_dff u2(.clk(clk), .d(out1), .q(out2));
    my_dff u3(.clk(clk), .d(out2), .q(q));
    

endmodule
